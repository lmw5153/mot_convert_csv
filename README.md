# mot_convert_csv


# OpenCap MOT → CSV 변환기 (웹)

- 여러 .mot/.sto 업로드 → CSV 변환
- "병합" 버튼으로 파일명(ID) 기준 세로 병합 (중복 ID는 _2, _3 자동 부여)

## 로컬 실행
```bash
pip install -r requirements.txt
streamlit run app_with_hero.py
